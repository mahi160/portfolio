---
import type { ArticleFrontmatter } from "../../lib/types";
import { processArticleDate } from "../../lib/utils";

type Props = {
  featuredArticles: Array<ArticleFrontmatter>;
}

const { featuredArticles } = Astro.props;
---

<section class="featured-writing">
  <div class="section-head">
    <div>
      <div class="section-kicker">Recent</div>
      <h2 class="section-title">Writing</h2>
    </div>
    <a href="/blog" class="arrow-link">All writing</a>
  </div>

  <div class="article-list">
    {featuredArticles.map((article) => (
      <a href={article.filename} class="article-card">
        <div class="article-meta">
          {processArticleDate(article.timestamp)}
          &nbsp;·&nbsp;
          {article.time < 1 ? "< 1" : Math.round(Number(article.time))} min read
        </div>
        <div class="article-title">{article.title}</div>
        <div class="article-desc">{article.description}</div>
      </a>
    ))}
    <div class="list-cap"></div>
  </div>
</section>

<style>
  .featured-writing {
    margin-bottom: 3.5rem;
  }

  .section-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    margin-bottom: 1.2rem;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .section-kicker {
    font-family: var(--mono);
    font-size: 0.72rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--ink-faint);
    margin-bottom: 0.35rem;
  }

  .section-title {
    font-size: 1.55rem;
    font-weight: 500;
    color: var(--ink);
    margin: 0;
  }

  .article-list {
    display: flex;
    flex-direction: column;
  }

  .article-card {
    display: block;
    padding: 1.1rem 0;
    text-decoration: none;
    border-top: 1px solid var(--line-soft);
    transition: background 160ms ease;
  }
  .article-card:hover { text-decoration: none; }

  .article-meta {
    font-family: var(--mono);
    font-size: 0.74rem;
    color: var(--ink-faint);
    margin-bottom: 0.55rem;
    letter-spacing: 0.04em;
  }

  .article-title {
    font-size: 1.15rem;
    font-weight: 500;
    line-height: 1.3;
    margin-bottom: 0.3rem;
    color: var(--ink);
  }

  .article-desc {
    color: var(--ink-muted);
    font-size: 0.9rem;
    line-height: 1.55;
  }

  .list-cap {
    border-top: 1px solid var(--line-soft);
  }
</style>
